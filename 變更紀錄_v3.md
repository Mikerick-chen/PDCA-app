# PDCA Time Master v3 — 變更紀錄書

> 基底：`v2/`（完整複製為 `v3/`，v2 原封不動以供對照與回滾）
> 撰寫日期：2026-06-10
> 對應需求：架構審查報告 v2 + 修改指南（依三項凍結決策）
> 進度：**第一批 🔴 已完成**；第二批 🟠 / 第三批 🟡 待核可後續做（見文末預告）

---

## 0. 凍結決策（本版實作的定錨，不可違背）

1. **資料持久化**：DB 改依賴 Zeabur「持久卷」，路徑由環境變數 `DB_PATH` 決定（程式已支援）；服務鎖單一副本。
2. **跨裝置近即時同步**：不引入 WebSocket；以「自動儲存 + 視窗聚焦/輪詢重抓 + 最後寫入者勝」達成。
3. **待辦清單本質**：嚴禁把前一日 Act 自動帶入隔日 Plan；改為在「週末 PDCA 下方」動態彙整當週週一～週五的 Act。

## 0.1 本版採用的決策（你已拍板）

- JWT 本機開發：**嚴格 fail-fast**（未設或為預設值一律拒絕啟動，本機亦需自設）。
- 自動儲存狀態：放 **PDCA 標題列右側**、**移除**手動「儲存」按鈕。
- 跨裝置 `softRefresh`：**安靜就地刷新**（不閃「載入中」、圖表用 `chart.update()`）。
- 選配項目**全部納入**：更新 `v3/SPEC.md`、`esc()` 補強跳脫、每日自動備份、樂觀鎖 409（任務 M）。

---

## Batch 0 — 建立 v3 與依賴

| 動作 | 說明 |
| --- | --- |
| 複製 | `v2/ → v3/`，排除 `data/`（資料庫）與 `node_modules/` |
| 安裝 | 於 `v3/` 執行 `npm install` |
| 新增套件 | `express-rate-limit`（唯一允許新增的 npm 套件，供第二批登入限流用）已寫入 `package.json` |

---

## 第一批 🔴 止血（資料活下來、不被偽造、不丟字）

### A. `.gitignore` 排除 `data/` 與 `node_modules/`
- **檔案**：`v3/.gitignore`
- **狀態**：自 v2 起即已包含 `node_modules/`、`data/`、`*.db`、`*.db-wal`、`*.db-shm`、`.env`，本版**原樣保留、無需改動**。
- **意義**：資料庫與 WAL 不進版控，避免資料外洩與合併衝突。

### B. JWT 密鑰啟動 fail-fast
- **檔案**：`v3/src/auth-middleware.js`
- **改動**：抽出原始碼預設密鑰常數 `DEFAULT_JWT_SECRET`；模組載入時若 `JWT_SECRET` **未設定或仍等於預設值**，印出明確錯誤後 `process.exit(1)`。
- **為何**：預設密鑰寫在公開原始碼，任何人都能用它簽出任意使用者 token（全站後門）。寧可起不來，也不可帶預設密鑰上線。
- **影響本機開發**：本機 `npm start` 前**必須**先設定 `JWT_SECRET`（見下方「本機啟動」）。

### C. Email 正規化（註冊／登入）
- **檔案**：`v3/src/routes/auth.js`
- **改動**：`register` 與 `login` 進入點先 `email = String(email).trim().toLowerCase()`，再做格式驗證、查詢與寫入。
- **為何**：SQLite `TEXT UNIQUE` 區分大小寫；`User@Gmail.com` 與 `user@gmail.com` 原會被當兩個帳號，或因大小寫不一而登不進去。
- **相容性**：僅應用層正規化，**未改 schema**（不加 `COLLATE NOCASE`，以維持與既有 `pdca.db` 完全相容）。

### D. Check／Act 自動儲存（取代手動按鈕、修無聲丟字）
- **檔案**：`v3/public/js/app.js`、`v3/public/css/styles.css`
- **改動**：
  - `#check`、`#act` 綁 `input`（停打 1.5 秒 debounce）自動送出 `PATCH /logs/:id`；`blur` 立即送出。
  - PDCA 標題列右側新增單一狀態指示 `#saveStatus`：「編輯中… / 儲存中… / 已儲存 ✓ / 儲存失敗」。
  - **移除**原本的「儲存檢核與行動」按鈕與 `savePDCA()` 函式（互動模型統一為自動儲存）。
  - `renderView()` 改為 `async`，切換視圖/日期**前先 `await flushCheckAct()`**：把待存內容立即送出並等待完成，確保切日期/切視圖不丟字（涵蓋日期切換器、前/後一日、左側導覽、行事曆點格、日記日期切換等所有路徑）。
  - 自動儲存成功後以回應更新 `state.currentLog`（含最新 `updated_at`），避免同裝置連續儲存自我衝突，並為第三批樂觀鎖鋪路。
  - CSS：新增 `.pdca-head`、`.save-status`（及 saving/saved/dirty/error 狀態色）；移除已不再使用的 `.save-bar`、`.save-hint` 規則（`.btn-save` 保留，仍用於日記/備忘錄/對話框）。

### E.（文件）Zeabur 端設定提示
> 此項你不需改碼，但部署時務必在 Zeabur 儀表板完成，否則「資料持久化」與「登入限流」不會真正生效。

1. **掛載持久卷（Persistent Volume）**：掛載路徑設為 `/app/data`。
2. **設定環境變數**：
   - `DB_PATH = /app/data/pdca.db`（讓資料庫落在持久卷上）。
   - `JWT_SECRET = <一段足夠長的隨機字串>`（**必填**，否則服務會 fail-fast 拒絕啟動）。
   - （建議）`CORS_ORIGIN = <你的前端網域>`，正式環境別用預設的全部允許。
3. **Replicas（副本數）鎖為 1**：單檔 SQLite 不可多副本，否則資料分裂（split-brain）。
4. **重新部署**：之後 redeploy / 重啟，資料都在卷上、不再蒸發。

---

## 如何驗證（第一批）

> 本機需先設 `JWT_SECRET`。以下以 PowerShell 為例。

```powershell
cd "v3"
$env:JWT_SECRET = "請改成一段夠長的隨機字串至少32字元"
npm start    # 開啟 http://localhost:3000
```

- **B（fail-fast）**：不設 `JWT_SECRET` 直接 `npm start` → 立刻印出錯誤並結束（exit 1）；設了才會啟動。
- **C（Email 正規化）**：用 `Foo@Gmail.com` 註冊；改用 `foo@gmail.com`（或大寫）登入應成功；再用任何大小寫變體重複註冊應回 `409`。
- **D（自動儲存）**：在 Check/Act 打字，**不要按任何按鈕**，標題列右側顯示「儲存中… → 已儲存 ✓」；切到別的日期再切回、或切到行事曆再回 PDCA，內容仍在。

---

## 風險與回滾（第一批）

| 風險 | 說明 | 緩解／回滾 |
| --- | --- | --- |
| 本機忘了設 `JWT_SECRET` | 服務不啟動 | 屬預期行為；錯誤訊息已附設定指令 |
| 自動儲存頻率 | 停打 1.5 秒或失焦才送出，非每次按鍵 | 已 debounce；如需更省可調大秒數 |
| 整批回滾 | — | v2 完全未動；捨棄 `v3/` 對應檔或整個 `v3/` 重來即可，不影響 v2 |

---

## 第二批 🟠 同步與新功能（已完成）

### F. 跨裝置近即時同步（`public/js/app.js`）
- 啟動時**註冊一次** `visibilitychange`、`window focus` 與每 20 秒輪詢，呼叫 `softRefresh()`。
- `softRefresh()`：焦點在 INPUT/TEXTAREA（使用者正在打字）或有對話框開啟時**一律跳過**，避免洗掉輸入；否則以 `renderView({ quiet:true })` **安靜就地刷新**目前視圖。
- 安靜刷新：不顯示「載入中」、不整頁重建版面；PDCA 改為就地更新 Plan/Do/Check、同步 Check/Act 文字、保留捲動與計時器。

### G. 降低同步成本（`resources.js`／`logs.js`／`app.js`）
- `PATCH /items/:id`、`DELETE /items/:id`（resources.js）與 `POST /logs/:id/items`（logs.js）的回應**一併回傳該日誌最新的 `plan_items` 與 `progress`**。
- 前端 `applyLogItems()` 據回應**就地更新**，移除原本「每次操作都 `GET /logs/:id`」的 `refreshItems()`。
- 完成度由前端就地計算（`recomputeProgress()`）。
- 勾選採**樂觀更新**：先改本地狀態與 UI、背景送出、失敗回滾並提示。
- Check 圖表改 `chart.update()`（canvas 未變時就地更新、不 `destroy()` 重建），並對圖表刷新做 ~500ms debounce（`scheduleChartRefresh()`）。

### H. 本週 Act 回顧
- 後端 `src/routes/user-data.js` 新增 `GET /week-acts?date=YYYY-MM-DD`：以該日期所在週回傳週一~週五各日 `act_content`（以日期字串星期計算、時區安全；沿用 `authRequired+sameUser`）。
- 前端 `renderPDCA` 中，`selectedDate` 為週六/日時於四宮格下方渲染面板（`renderWeekendActReview()`），逐列顯示週一~五 Act、點列跳該日；週末當天自身的「Act 行動」框即作為本週總結（**不新增資料表**）。
- 樣式 `public/css/styles.css` 新增 `.weekend-review` 等（沿用既有設計變數與配色）。

### I. 登入限流（`server.js` + `express-rate-limit`）
- 對 `/api/v1/auth` 套用 `express-rate-limit`：每 IP 15 分鐘最多 30 次（登入+註冊合計），逾限回 `429` 並維持統一回應信封。
- 加 `app.set('trust proxy', 1)`：Zeabur 等反向代理後方才能取得真實用戶端 IP。

### J. 計時器跨視圖修復（`public/js/app.js` `bindTimer`）
- 將剩餘秒數與運行狀態抽至模組層 `timerState`（運行時以「結束時間戳 `endsAt`」推算剩餘秒，切視圖/分頁回來仍準確）。
- `renderView()` 切換時 `clearTimerInterval()`，重繪後 `bindTimer()` 依 `timerState` 還原並續跑，修正「切視圖後計時器卡死」。

### 每日自動備份（`server.js`）
- 啟動後 10 秒做一次、其後每 24 小時一次 `VACUUM INTO`，匯出帶日期副本至資料庫同層 `backups/`，保留最近 7 份。

### 第二批驗證（已實機跑過）
- **G**：`POST/PATCH/DELETE` 項目回應均含 `plan_items`+`progress`；勾 1/2 → 50%、刪除後 1/1 → 100%。✅
- **H**：`GET /week-acts?date=2026-06-13`（週六）→ `week_start=2026-06-08`，回傳週一~五五筆 Act；瀏覽器中週六日期下方面板顯示 5 列、標題正確。✅
- **I**：連續登入至第 31 次 auth 請求 → `429`。✅
- **備份**：啟動後產生 `data/backups/pdca-YYYY-MM-DD.db`。✅
- **前端**：登入、PDCA 完整渲染、Chart 實例建立、無 console error（瀏覽器驗證）。✅
- **待你於瀏覽器確認**：計時器跑著切到行事曆再切回仍續走（J）、勾選樂觀更新即時反映（G）、B 裝置切回分頁 ≤20 秒看到 A 的更新且打字中不被洗掉（F）。

### 第二批風險與回滾
| 風險 | 緩解 |
| --- | --- |
| 20 秒輪詢的請求量 | 僅重抓目前視圖、安靜刷新；弱網可調大間隔（`startSync` 內 20000） |
| 樂觀更新與伺服器不一致 | 每次操作都以伺服器回應 `applyLogItems()` 校正；失敗回滾 |
| 限流誤傷 | 30 次/15 分鐘為登入+註冊合計，正常使用充足；可於 `authLimiter` 調整 |
| 備份磁碟占用 | 僅保留最近 7 份、自動刪舊 |
| 回滾 | v2 未動；個別檔案可單獨還原 |

---

## 第三批 🟡 體質（已完成）

### K. `isValidDate` 收嚴（`src/helpers.js`）
- 在格式檢查與 `NaN` 檢查之外，再以 `parse 後 toISOString().slice(0,10)` 與原字串比對，擋掉 `2026-02-30`、`2026-04-31` 這類「格式對但日子不存在」的幽靈日期（回 `400`）。僅影響新寫入，既有資料不受影響。

### L. Plan_Items「刻意不跨日帶入」註解
- 在 `src/routes/logs.js`（`POST /:id/items`）與 `public/js/app.js`（`renderPlanList` 上方）加上明確繁中註解：計畫項目永遠只屬當日，禁止日後加入「昨日 Act→今日 Plan」之類跨日帶入邏輯；跨日彙整只透過唯讀的「本週 Act 回顧」呈現。

### M. 樂觀鎖 409（`src/routes/logs.js` + `public/js/app.js`）
- 後端 `PATCH /logs/:id`：若請求帶 `updated_at` 且與現況不符 → `409 CONFLICT`，並於 `details.current` 附上伺服器最新版。
- 前端 `api()` 在錯誤物件帶上 `status/code/details`；自動儲存改帶 `updated_at`；收到 409 時 `handleLogConflict()` 以最新版重載，**但不覆蓋使用者正在編輯的欄位**（避免吃字），若使用者仍有未存編輯則標回 dirty、下次以新 `updated_at` 重試（最後寫入者勝）。

### esc() 補強跳脫（`public/js/app.js`）
- `esc()` 加入 `'`→`&#39;`、`` ` ``→`&#96;`，避免值被放進單引號/反引號屬性時破防。

### 文件
- 更新 `v3/SPEC.md`：新增「附錄 B：v3 增補」，記錄持久化/備份、JWT fail-fast、email 正規化、限流、自動儲存/同步、`/week-acts`、待辦本質、日期驗證、側欄收合等規格增補。

---

## 額外 UI 需求：左側選單可收合（折頁）

> 你回報「左側切換在使用時占空間，希望能縮起來」，於本版一併實作。

- **檔案**：`public/js/app.js`、`public/css/styles.css`
- **行為**：頂部列最左新增漢堡鈕（`#btnNav`）；點擊在 `.app` 切換 `nav-collapsed` 類別，側欄寬度由 132px（或行動裝置 64px）平滑收為 0、主內容自動補滿；再點一次展開。
- **記憶偏好**：收合狀態存於 `localStorage`（`pdca_nav_collapsed`），重新整理後維持。屬純 UI 狀態，**不影響任何業務資料**（符合 SPEC 第 0 節對 localStorage 的允許範圍）。
- **可調整**：目前收合為「完全隱藏」以最大化空間；若你偏好「只縮成圖示細條（仍可一鍵切換）」我可改成 icon-rail 版。

---

## 第三批 + UI 驗證
- **K**：`POST /logs/init` 以 `2026-02-30` → `400`；以 `2026-06-15` → 正常。✅
- **M**：帶過期 `updated_at` 的 `PATCH /logs/:id` → `409`，`details.current` 含最新版；帶正確 `updated_at` → `200`。✅
- **esc()**：含 `'` 與 `` ` `` 的輸入經 `esc()` 後輸出為實體編碼。✅
- **側欄收合**：點漢堡鈕側欄收合/展開、重新整理維持偏好（瀏覽器驗證）。✅

## 第三批 + UI 風險與回滾
| 風險 | 緩解 |
| --- | --- |
| 日期驗證變嚴可能擋掉舊流程送的怪日期 | 屬預期；正常 `YYYY-MM-DD` 不受影響 |
| 樂觀鎖 409 造成自動儲存偶發提示 | 僅「他處先存了」才觸發；前端自動重載並以新版重試，不丟字 |
| 側欄收合在 ≤380px 的頂列空間 | 漢堡鈕僅一顆圖示，影響極小；可再依需求調整頂列 |
| 回滾 | v2 未動；個別檔案可單獨還原 |

---

## 本機啟動（重申）
```powershell
cd "C:\Users\user\Desktop\PDCA App\v3"
$env:JWT_SECRET = "請改成一段夠長的隨機字串至少32字元"
npm start    # http://localhost:3000
```
未設 `JWT_SECRET` 會 fail-fast 拒絕啟動（這是刻意的安全設計）。

---

## UI 視覺優化（依「增加反饋與層次感」設計建議）

> 依 `UI設計：增加反饋與層次感 - Google Gemini.pdf` 的三大策略實作。**全為純 CSS**（搭配計時器一個 `.running` class），
> 不更動任何功能、API 或資料；在不破壞既有清晰度與專業感的前提下，增加層次與手感。
> 檔案：`public/css/styles.css`、`public/js/app.js`（計時器 class）、`public/index.html`（cache 版本 `?v=3`）。

### 策略一 立體層次（Visual Depth）
- 背景由 `#f4f5f7` 改為略壓暗的冷灰 `#e9ebf1`，四宮格保持純白 → 卡片明顯「跳」出。
- 新增多層次柔和漸層陰影變數（`--shadow-card/-hover/-sm`），套用於 PDCA 卡、日記/備忘錄卡、計時器、週末面板、對話框；頂列加柔和投影。
- 圓角統一上調至 `12px`，更溫潤。

### 策略二 紋理與氛圍（Texture & Atmosphere）
- **相紙微顆粒**：`body::after` 疊一層 `feTurbulence` 雜訊（透明度 ~2.2%、`pointer-events:none`），中和純色死板。
- **點陣手帳感**：背景隱約點陣（24px 間距、極淡），呼應「規劃／邏輯」工具的筆記本隱喻。
- **環境微光暈**：左上角淡藍、右下角淡橘的超大模糊品牌色光暈（固定於視窗），營造空間氛圍。

### 策略三 微交互反饋（Micro-interactions）
- **物理質感按鈕**：主要按鈕 hover 微上浮加深陰影、`active` 下壓微縮（`scale(0.96)`），模擬實體按鍵。
- **狀態進場引導**：切到 PDCA／換日期時，四宮格依序淡入上浮；週末面板、對話框、登入卡皆有柔和進場（輪詢的安靜刷新不會重播，無閃爍）。
- **清單卡 hover 輕浮**：日記/備忘錄卡與行事曆格 hover 上浮加陰影；側欄項目 hover 輕微右移（手感）。
- **會呼吸的計時器**：運行中數字轉為品牌綠並極微脈搏（`breathe` 動畫）、外框加內光環，讓系統「活著」。
- **無障礙**：尊重 `prefers-reduced-motion`，偏好減少動態時自動停用動畫/過渡。

### 驗證（瀏覽器實機）
- PDCA 四宮格白卡浮起、背景點陣與光暈可見、版面清晰未破版。✅
- 計時器開始 → 數字轉綠並套用 `breathe` 動畫、持續倒數（00:25:00 → 00:24:43）。✅
- 側欄收合與新視覺並存、主內容補滿。✅　無 console error。✅

### 風險與回滾
- 純樣式 + 一個計時器 class，無功能風險；`prefers-reduced-motion` 已保護動暈敏感使用者。
- 若覺得紋理過重，可調 `body` 背景的 `rgba` 透明度或 `body::after` 的 `opacity`；要全部還原只需移除 `styles.css` 末段「v3 視覺優化」區塊並把 `--bg/--radius` 改回。

---

## 修正：重新載入時出現方塊（圖示字型）＋ 註冊欄位順序

### 1) 圖示方塊（豆腐字）→ 改為本地自帶圖示字型
- **症狀**：重新載入時，`<i class="ti ...">` 圖示短暫（或在 CDN 不穩時持續）顯示為方塊 □，登入鈕甚至出現亂碼字元。原因為 Tabler 圖示字型走 CDN，下載前瀏覽器以豆腐字佔位（也是審查報告點名的「硬依賴 CDN」風險）。
- **修正**：
  - 將 Tabler 圖示字型自帶於專案：`public/vendor/tabler/tabler-icons.min.css` 與 `public/vendor/tabler/fonts/tabler-icons.woff2`（同源、可快取、離線可用）。
  - `@font-face` 設 `font-display: block`（載入期間顯示「空白」而非方塊），並只保留 woff2 來源。
  - `index.html`：移除 CDN `<link>`，改引用本地 CSS，並對 woff2 加 `<link rel="preload" ... crossorigin>` 讓字型最早下載。
- **驗證**：本地 CSS/woff2 皆 `200`、字型 `font/woff2`、`document.fonts` 顯示 tabler 字型 `loaded`；登入/註冊頁所有圖示正常、無方塊、無亂碼。✅
- **附註**：Chart.js 仍走 CDN（圖表載不到只會「沒有圖表」，不會產生方塊）；若你要也一併本地化，我可再處理。

### 2) 註冊欄位順序：姓名 ↔ Gmail 互換
- **修正**：`renderAuth` 中註冊模式的欄位順序改為 **姓名 → Gmail → 密碼**（登入模式仍為 Gmail → 密碼），輸入動線更順。
- **驗證**：註冊頁欄位 label 依序為「姓名 / Gmail / 密碼（至少 8 碼）」。✅

### 檔案
- 新增：`public/vendor/tabler/tabler-icons.min.css`、`public/vendor/tabler/fonts/tabler-icons.woff2`
- 修改：`public/index.html`（本地字型 + preload）、`public/js/app.js`（註冊欄位順序）

---

## 新功能：頂部頭像「多帳號快速切換」

> 需求：頂部頭像可直接切換「已登入過的帳號」（免再輸入密碼）。

- **檔案**：`public/js/app.js`、`public/css/styles.css`
- **做法**：
  - 新增「已登入帳號池」`localStorage.pdca_accounts = [{id,email,display_name,token}]`；登入成功即 `upsertAccount` 記住該帳號（升級自舊版時，`boot()` 會把目前帳號補進池）。
  - 頭像改為按鈕，點擊開啟下拉選單：顯示目前帳號、可切換的其他帳號（一鍵 `switchAccount` 直接切換、免密碼）、「登入其他帳號」（保留池、前往登入頁新增）、「登出目前帳號」。
  - `logout()` 改為「登出目前帳號」語意：移出池後，若還有其他帳號則直接切過去，否則回登入頁；`api()` 收到 401 亦走此邏輯（token 失效 → 換下一個帳號或重新登入）。
- **安全/隱私註記**：多個 token 存於 `localStorage`，與既有單一 token 的儲存方式一致（審查報告已知的取捨；若日後改用 httpOnly cookie，這裡一併調整）。可在選單以「×」將帳號移出清單。
- **驗證（瀏覽器實機）**：種兩個帳號（Alice/Bob）→ 頭像選單顯示目前 Alice、可切換 Bob；點 Bob → 頭像 A→B、`pdca_user` 變 Bob、選單自動關閉、**免輸入密碼**；圖示正常、無 console error。✅

---

# 讀書會（群組讀書計時）— Batch S1（後端，已完成）

> 依《讀書會功能_架構設計.md》§11 的 MVP 計畫第一批。**只新增資料表與路由，既有功能與資料零更動**；無新 npm 套件（限流用既有的 express-rate-limit）。

## 變更檔案
- `src/db.js`：新增 3 張表 + 索引（`CREATE TABLE IF NOT EXISTS`）。
  - `Study_Sessions`（讀書時段）、`Study_Groups`（群組；命名避開 SQLite `GROUPS` 保留字）、`Group_Members`（成員，`UNIQUE(group_id,user_id)`）。
- `src/auth-middleware.js`：新增 `requireGroupMember` / `requireGroupOwner`（本功能唯一的「跨使用者讀取」授權；用語意正確的 403）。
- 新檔 `src/routes/study.js`：`POST /study/sessions/start`、`/:id/stop`；`GET /study/me`、`/study/summary`。（**依需求移除心跳**：無 `/heartbeat` 端點、`Study_Sessions` 移除 `last_beat_at` 欄位。）
- 新檔 `src/routes/groups.js`：`POST /groups`、`GET /groups`、`POST /groups/join`（限流）、`GET /groups/:id`、`PATCH /groups/:id`、`DELETE /groups/:id`、`DELETE /groups/:id/leave`、`DELETE /groups/:id/members/:user_id`。
- `server.js`：掛載 `/api/v1/study`、`/api/v1/groups`（置於 `/api/v1` resource 路由之前）。

## 關鍵設計
- **時數防作弊**：讀書時數一律由伺服器時間戳（`started_at`/`ended_at`）計算，忽略前端宣稱秒數。
- **無心跳（依需求移除）**：計時器＝明確「開始/結束」碼錶；進行中段的即時秒數由前端依 `started_at` 自行累加。
- **單一進行中 session**：重複 `start` 會回傳同一段（可更新科目），不產生重複 active。
- **遺留段惰性結算**：`me`/`summary`/`start` 時，把「開始已逾 6 小時（MAX_SESSION）」的進行中段自動封頂結算（處理關分頁未按結束），不需排程。
- **取捨**：無心跳時，關分頁未按「結束」者，在看板（S3）可能仍顯示讀書中，直到他按結束或 6 小時上限自動結算。
- **邀請碼**：6 碼、去除易混淆字元、唯一；`join` 端點限流（10 分鐘 30 次）。
- **owner 退出**：自動轉移給最早加入的成員；無人則解散（CASCADE 清成員）。

## 驗證（API 煙霧測試，已實機跑過）
- 讀書：start(running)→重複 start 回同一段→me(finished_seconds/active)→stop(ended、duration 計算)→summary(每日彙總)。✅
- 群組：create(owner,1 人)→list→join(member,2 人)→detail(成員清單 Alice owner / Bob member)。✅
- 授權：非成員看詳情 403、非 owner 改群組 403、owner 改群組 200；踢人 200 後被踢者 403；owner 退出→自動轉移、新 owner 角色正確。✅

## 風險與回滾
- 只新增資料表（不動既有 6 張表），完全相容既有 `pdca.db`；要回滾移除兩個 route 檔、`server.js` 兩行掛載、`db.js` 新增區塊、`auth-middleware.js` 兩個函式即可。
- 前端尚未接（無 UI）；此批純後端，既有 v1~v3 功能不受影響。

## 待辦
- （讀書會 MVP 三批已全部完成；後續可選：每日目標、互相加油 Cheer、公開群組等 Phase 2）

---

# 讀書會 — Batch S2（前端「讀書會」分頁，已完成）

> 前端：`public/js/app.js`、`public/css/styles.css`、`public/index.html`（cache 版本 `?v=4`）。**依需求未做心跳**。

## 內容
- 左側導覽新增「讀書會」分頁（`ti-users-group`）。
- **讀書計時器**（碼錶，無心跳）：大碼錶顯示、科目輸入、「開始讀書／結束」切換、「今日已讀」總時。
  - 即時秒數由前端依 `active.started_at`（伺服器時間）本地累加；切回分頁/重新整理時，依 `GET /study/me` 的 `active` 自動續顯（伺服器為準）。
  - 計時中數字轉品牌綠；切離分頁時 `renderView` 會清掉本地 tick interval，回到分頁再重建。
- **群組管理**：我的讀書會卡片列（名稱/描述/成員數/角色/邀請碼）；建立群組、輸入邀請碼加入、複製邀請碼、查看成員（modal）、編輯/解散（owner）、離開（成員）、踢除成員（owner）。
- 讀書會分頁**暫不納入 20 秒輪詢**（即時看板於 S3 才加）；計時器在本地獨立累加，不受影響。

## 驗證（瀏覽器實機）
- 分頁渲染、空狀態正確。✅
- 計時器：開始 → 數字逐秒累加（00:01→00:02）、轉綠、按鈕變「結束」；結束 → 歸零、「今日已讀」累計實際秒數（伺服器時間戳，跨多次操作仍正確）。✅
- 建立群組 → 卡片出現（晨讀會／擁有者／1 位成員／邀請碼）；成員 modal 顯示「讀書人 擁有者」。✅
- 圖示正常、無 console error。✅

## 風險與回滾
- 純前端 + 後端已於 S1 完成；要回滾移除 app.js 的讀書會區塊、CSS、導覽項與 `renderView` 分支即可。
- 既有 v1~v3 與 PDCA 等功能不受影響。

---

# 讀書會 — Batch S3（即時看板 + 排行榜，已完成）＋ 科目可選計畫

> 後端：`src/routes/groups.js`（board/leaderboard）、`src/routes/logs.js`（新增唯讀 `GET /logs/by-date`）；前端：`app.js`、`styles.css`、index.html `?v=5`。

## 內容
- 後端
  - `GET /groups/:id/board?date=`：每位成員的 `status`（讀書中/閒置）、`current_subject`、`live_seconds`、`today_seconds`，依今日總時排序 + 群組今日總時（限成員）。「讀書中」＝有進行中段且開始未逾 6 小時。
  - `GET /groups/:id/leaderboard?period=day|week&date=`：期間內「已結束」讀書總時數排行（week 以該週週一~週日）。
  - `GET /logs/by-date?date=`：唯讀取當天日誌（查無回 null、**不建立空日誌**），供科目下拉帶入 Do/計畫內容。
- 前端「讀書會」分頁升級為：讀書計時器 + **群組切換 chips** + **選取群組的即時看板**（今日看板 / 本週排行 兩分頁）。
  - 今日看板：成員列含名次、綠色脈動「讀書中」狀態點、目前科目、即時秒數、今日總時。
  - 本週排行：🥇🥈🥉 名次 + 各成員本週已完成讀書時數。
  - **~15 秒輪詢**只刷新看板列表（不動標題/分頁，避免閃爍）；分頁隱藏、非讀書會視圖、有對話框時自動暫停；離開視圖清除輪詢。
- **科目欄位**（依你的需求）：改為 `datalist` 下拉——**可自由輸入，也可點選帶入當天 Do（計畫項目）內容**。

## 驗證（瀏覽器實機，雙使用者）
- 種 Alice/Bob 同群組、Alice 讀書中；以 Bob 登入看板 → 看到「Alice 艾莉絲 · 數學　讀書中 00:00:14」(名次1、綠點)、Bob 閒置(名次2)。✅
- Bob 也開始讀書（英文）→ 看板兩人皆「讀書中」、依時間排序。✅
- 本週排行：Bob 結束一段後 🥇15 秒、Alice 🥈0 秒（排行只計已完成段）。✅
- 科目 datalist：今日新增計畫「複習錯題 / 背單字」→ 科目下拉即出現這兩個選項，且仍可自填。✅
- 無 console error。

## 風險與回滾
- 看板/排行為唯讀查詢（限群組成員）；輪詢只刷新列表、失敗靜默。
- 取捨（無心跳）：關分頁未按「結束」者在看板可能持續顯示「讀書中」，直到他按結束或滿 6 小時上限自動結算。
- 回滾：移除 groups.js 的 board/leaderboard 兩路由、logs.js 的 `by-date`、app.js 看板相關函式與 CSS 即可；S1/S2 與既有功能不受影響。

---

# 企業級精進 — D1：設計系統 Token + 字級比例尺 + tabular-nums（已完成）

> 純 CSS 地基重構（`public/css/styles.css`、index.html `?v=6`）。**不動 app.js**：既有 CSS 變數名全部保留，app.js 內聯 `var(--…)` 照常運作。

## 內容
- **語意化 Token 系統**（`:root`）：把散落的裸 hex 收斂為語意層 —
  - 中性面：`--bg / --surface / --surface-2 / --surface-hover / --surface-active / --border / --border-strong`
  - 文字三階：`--text(主) / --text-muted(次) / --text-3(弱)`，皆 ≥ WCAG AA 4.5:1
  - 品牌：`--accent / --accent-text(小字安全) / --accent-fg / --accent-weak-bg / --accent-strong`
  - 狀態：`--success/-text/-weak-bg`、`--danger/-text/-weak-bg`、`--warning/-text/-weak-bg`
  - 幾何：`--radius-sm/-/-lg/-pill`；間距尺 `--sp-1…8`（8pt 基準，供逐步採用）
  - 字級尺：`--fs-caption/label/body/h3/title/display`、`--lh-*`、`--fw-*`
- **元件去裸色**：全檔 29 處硬寫狀態色(hover/selected/danger/warning 底、深藍文字、淺灰)改引用 token；單一真實來源在 `:root`（為日後深色模式鋪路）。
- **修正對比(WCAG AA)**：
  - `--text-muted` 由 `#6b7280` 加深為 `#5b626e`（穩過 AA）。
  - 小字綠 `已儲存✓`、看板 `讀書中 mm:ss` → `--success-text #157a5b`；藍色連結 `立即註冊` → `--accent-text #2767b0`。
  - 過淺的 placeholder / 空狀態 `#b8bdc7` → `--text-3 #8b919c`。
- **字級基準 + 等寬數字**：`body` 設 `--fs-body/—lh-normal`；所有數字/時間（計時器、看板秒數、完成度%、統計值、行事曆日期/百分比）加 `font-variant-numeric: tabular-nums`，跳動不抖。`.view-title` 改用 token。

## 驗證（瀏覽器實機）
- 所有 token 於 `getComputedStyle(:root)` 正確解析（無空值/無循環自我參照）。✅
- 連結 `立即註冊` 計算色 = `rgb(39,103,176)`（#2767b0，AA 通過）；`body` 字級 = 14px。✅
- PDCA 等視圖外觀無回歸（白卡、左側類別色條、選取態 `--accent-weak-bg`、陰影、紋理皆正常）；無 console warning/error。✅

## 風險與回滾
- 純樣式、值幾乎等價（僅 muted/小字/placeholder 變深以過 AA，屬刻意改善）。
- 回滾：將 `:root` 還原為舊變數、把 token 用法改回 hex 即可；或整檔還原 styles.css。app.js 不受影響。

## 後續（同企業級路線圖）
- D3 Toast/確認框/Skeleton/狀態全套、D4 a11y、D5 ⌘K、D6 RWD/分頁/圖示子集化。

---

# 企業級精進 — D2：深色模式（已完成）

> 建立在 D1 token 之上。檔案：`public/css/styles.css`、`public/js/app.js`、`public/index.html`（cache `?v=8`）。

## 內容
- **主題機制**：以 `<html data-theme="light|dark">` 套用；偏好存 `localStorage.pdca_theme`（`light/dark`，初次未設時跟隨作業系統 `prefers-color-scheme`）。
- **深色 token**：新增 `:root[data-theme="dark"]` 覆寫整套 D1 token（深色面/邊框、淺色三階文字、品牌與狀態色、各狀態淡底、PDCA 類別標題淺色版、深色弱化陰影），並改寫深色的全頁紋理（淺色點陣 + 調暗光暈）。`color-scheme` 同步切換，使原生控制項（日期選擇器、捲軸、下拉）也深色化。
- **無閃爍(no-FOUC)**：`index.html <head>` 內聯小腳本在套用樣式前先設好 `data-theme`，避免重新整理時白光一閃。系統偏好變更時即時跟隨（`matchMedia` 監聽，僅在 pref 未明確指定時）。
- **一鍵切換鈕（依你的要求改放頂部列）**：移除帳號選單裡的「外觀」段；改在**頂部列**放一個「方框」按鈕，淺色顯示**月亮**（點了變深色）、深色顯示**太陽**（點了變淺色），標題同步提示。
- **連帶 token 化**：把先前仍硬寫的 PDCA 類別標題色、倒數膠囊、進度條軌、週末回顧底、警示閃爍、info/err 提示等 13+ 處改引用 token；statRow 的「今日完成度」inline 色改 `var(--check-strong)`，深色下自動轉淺金。

## 驗證（瀏覽器實機）
- 一鍵鈕：淺色=月亮「切換為深色」→ 點擊 → 深色=太陽「切換為淺色」，雙向皆正常、偏好持久化（reload 維持）。✅
- 深色 token 經 `getComputedStyle` 確認：頁底 `#0f1115`、卡片 `#181b21`、文字 `#e7e9ee`、邊框 `#2a2f38`、Plan 標題 `#6fb0ee`；切換鈕亦正確使用深色面/邊框 token。✅
- 帳號選單已移除「外觀」段（`acctTheme` 不存在）。✅　無 console error。
- （備註：預覽工具截圖偶發逾時，屬其 renderer 狀態問題；本批以 DOM/computed 值驗證，結果一致正確。先前 D2 深色 PDCA 截圖已可佐證觀感。）

## 風險與回滾
- 主題為純 token 覆寫 + 一個 `<html>` 屬性；回滾移除 `:root[data-theme="dark"]` 區塊、頂部切換鈕與三個主題函式即可，淺色維持原狀。
- 已知小項（非阻斷，列 D 後續）：Check 圖表（Chart.js）的格線/座標文字顏色仍由 JS 寫死，深色下偏淡；要完全主題化需於 `loadCheckChart` 讀 CSS 變數重繪。

---

# 企業級精進 — D3：Toast / 確認框 / 骨架載入 / 互動狀態（已完成）

> `public/js/app.js`、`public/css/styles.css`、index.html `?v=9`。消滅原生 `alert/confirm` 的廉價感、降低感知延遲、補齊狀態可見性。

## 內容
- **Toast（非阻斷通知）**：新增 `toast(msg, type)`（info/success/error，右下角浮出、自動消失、可點關、`aria-live="polite"`）。取代全部 6 處 `alert(e.message)`。
- **樣式化確認框**：新增 `confirmDialog(msg, {confirmText, danger})` 回傳 Promise；支援 **ESC 取消 / Enter 確認 / 點遮罩取消**、危險動作紅色確認鈕。取代全部 3 處 `window.confirm`（解散/離開/踢除）。
- **骨架載入（Skeleton）**：新增 `skeletonPDCA/Cards/Calendar/BoardRows` + shimmer 動畫；取代 6 處「載入中…」純文字（PDCA、行事曆、日記、備忘錄、讀書會、看板列）。零版面位移、感知更快。
- **互動狀態補齊**：
  - `:focus-visible` 統一鍵盤焦點環（按鈕/連結/導覽/chips/分頁/格/輸入）。
  - `button:disabled` 統一禁用樣式。
  - **載入中（busy）**：新增 `withBusy(btn, fn)`，非同步期間按鈕禁用 + 轉圈，完成還原；套用於登入/註冊、建立群組、加入群組、開始/結束讀書（防連點、給回饋）。
- 全部尊重 `prefers-reduced-motion`（shimmer/spin 在偏好減少動態時停用）。

## 驗證（瀏覽器實機，DOM 斷言）
- 切到讀書會時出現 `.skel` 骨架、載入後消失、無「載入中」文字。✅
- 點「解散」→ 樣式化確認框（紅色「解散」鈕、訊息正確），按「取消」→ 關閉且群組保留。✅
- 製造真實錯誤（伺服器端先刪群組）後點「成員」→ 跳 `toast-error`「你不是此群組的成員」，`aria-live` host 存在。✅
- 點「開始讀書」→ 按鈕進入 `is-busy` + `disabled`，完成後還原、計時開始。✅　無 console error。

## 風險與回滾
- 純前端疊加；回滾移除 D3 的 helper 函式、CSS 區塊，並把 `toast/confirmDialog/skeleton/withBusy` 用法改回原生 `alert/confirm`、「載入中…」即可。

---

# 企業級精進 — D5：⌘K 命令面板（已完成）

> `public/js/app.js`、`public/css/styles.css`、index.html `?v=10`。生產力工具的招牌功能。

## 內容
- **快捷鍵**：全域 `⌘K`（Mac）/ `Ctrl+K`（其他）開啟/關閉；頂部列另加一顆「搜尋」樣式觸發鈕（顯示 `⌘K`/`Ctrl K` 提示，窄螢幕只留圖示）。只在登入後可用。
- **面板**：置中浮層 + 搜尋輸入（自動聚焦）+ 分組命令清單；支援**鍵盤上下選取、Enter 執行、Esc 關閉、點遮罩關閉、滑鼠 hover 選取、即時關鍵字過濾**（比對標籤/關鍵字/分組，含中英關鍵字）。`role="dialog"/listbox/option`、`aria-modal`。
- **17 條命令**（依現有功能，分四組）：
  - 前往：PDCA / 行事曆 / 日記 / 備忘錄 / 讀書會
  - 日期：跳到今天 / 前一天 / 後一天
  - 新增：日記 / 備忘錄 / 重要日期 / 建立讀書會 / 加入讀書會（會先切到對應視圖再開對話框）
  - 工具：專注時鐘 / 切換深淺色 / 收合側欄 / 登出
- 連帶把側欄收合邏輯抽成 `toggleNav()`，供按鈕與命令共用。

## 驗證（瀏覽器實機，DOM 斷言）
- `Ctrl/⌘+K` 與頂部鈕皆可開啟，輸入框自動聚焦，17 條命令、分組「前往/日期/新增/工具」。✅
- 輸入「讀書」→ 過濾出「讀書會 / 建立讀書會 / 加入讀書會」。✅
- 在過濾結果按 **Enter** → 執行「讀書會」、面板關閉、視圖切到讀書會。✅
- `⌘K` 再開、`Esc` 關閉皆正常。✅　無 console error。
- （備註：預覽截圖在有全螢幕浮層時偶發逾時，屬預覽工具 renderer 問題；本批以 DOM 斷言完整驗證。）

## 風險與回滾
- 純前端疊加；回滾移除 D5 區塊（命令函式、頂部鈕、`registerCmdk()`、CSS）即可，其餘功能不受影響。

---

# 企業級精進 — D4：無障礙（a11y）深化（已完成）

> `public/js/app.js`、`public/css/styles.css`、index.html `?v=11`。純前端、不改任何資料/API。

## 內容
- **浮層焦點管理（最大項，DRY）**：以 `MutationObserver` 統一處理所有 `modal-mask` / `cmdk-mask` —— 開啟自動聚焦第一個可聚焦元素、`Tab/Shift+Tab` 焦點鎖在浮層內、`Esc` 關閉（呼叫該浮層的取消鈕）、**關閉後把焦點還給開啟前的元素**；並補 `role="dialog"` + `aria-modal="true"`。
- **跳過導覽連結**：鍵盤 Tab 第一站「跳到主要內容」(`.skip-link`)，平時隱藏、聚焦時滑入，跳至 `<main id="main" tabindex="-1">`。
- **地標與狀態**：`<nav aria-label="主要導覽">`、作用中導覽項 `aria-current="page"`（切換時同步更新）；側欄收合鈕 `aria-expanded`、頭像鈕 `aria-haspopup`+`aria-expanded`、深淺色鈕動態 `aria-label`；PDCA 儲存狀態 `aria-live="polite" role="status"`（螢幕報讀器會念「已儲存」）。
- **可點 div 鍵盤化**：行事曆日格、週末回顧列、帳號切換列改用 `onActivate()`（補 `role="button"`+`tabindex="0"`+Enter/Space），行事曆日格另加 `aria-label`（日期＋用途）。
- **表單欄位命名**：所有僅靠 placeholder 的輸入（日記/備忘錄/群組/邀請碼/重要日期/計畫項目/讀書科目/日期選擇）補上 `aria-label`。
- **圖示按鈕命名**：頂部列各 icon 鈕（選單、命令、倒數、前/後一天、時鐘、登出）補 `aria-label`。
- （focus-visible 焦點環、`prefers-reduced-motion` 已於 D1/D3 具備，`[tabindex]` 一併涵蓋新可聚焦元素。）

## 驗證（瀏覽器實機，DOM 斷言）
- 外殼：skip-link（→#main）、`nav[aria-label]`、作用中 `aria-current=page`、`main[tabindex=-1]`、`btnNav[aria-expanded]`、`btnTheme[aria-label]`、`btnAvatar[aria-haspopup]`、`saveStatus[aria-live=polite]` 全部正確。✅
- 浮層（重要日期 modal）：`role=dialog` + `aria-modal`、開啟焦點移入、**Esc 關閉並把焦點還回觸發鈕**。✅
- 行事曆日格：`role=button` + `tabindex=0` + `aria-label`「2026-06-01，前往該日 PDCA」；切到行事曆後該導覽項 `aria-current=page`。✅　無 console error。

## 風險與回滾
- 純前端疊加；回滾移除 D4 的 `setupOverlayA11y`/`onActivate`、各 `aria-*` 屬性與 `.skip-link` CSS 即可。

---

# 企業級精進 — D6（RWD + 清單分頁）＋ 圖表深色化（已完成）

> `public/js/app.js`、`public/css/styles.css`、`src/routes/user-data.js`、index.html `?v=13`。

## 1) Check 圖表深色化
- `loadCheckChart` 改用 `chartColors()`（依 `data-theme` 給格線/座標文字色）；新增 `applyChartTheme()`，切換主題時於 `applyTheme()` 即時重塗（`chart.update('none')`）。修正 x 軸刻度色未更新的問題（先前誤把 Chart.js 的 options proxy 重新賦值；改為直接改 `.color`）。
- 驗證：淺色 grid `#eef0f3`／ticks `#6b7280`；切深色後 grid `rgba(255,255,255,0.08)`、**x/y 軸刻度同時**轉 `#a3abb8`。✅

## 2) RWD 響應式成熟度
- 新增中斷點：**≤560px** 頂部列收斂（倒數膠囊只留圖示、日期欄上限、縮小間距/內距、計時器數字縮小）；**≤430px** 再隱藏日期前/後鈕（日期欄與 ⌘K 仍可換日）；**≥1280px** 內容寬上限放寬至 960。
- 驗證：380/476px 下**頁面無水平捲動**、倒數文字與 ⌘K 提示正確隱藏。✅（呼應 SPEC ≤380px MUST）

## 3) 備忘錄清單分頁（避免年復一年一次撈全部）
- 後端 `GET /users/:id/memos?limit=&offset=` 改回傳 `{ items, has_more, offset, limit }`（多取一筆判斷）；預設每頁 24。
- 前端 `renderMemos` 改分頁：首頁 24 筆 + 「載入更多」（`withBusy` 轉圈、追加卡片、用 `memoById` 對映讓追加卡片仍可編輯/置頂/刪除）；備忘錄分頁不納入 20 秒輪詢（避免重置）。
- 驗證：種 30 筆 → 首頁 24 + 「載入更多」→ 點擊追加至 30、按鈕自動消失。✅　無 console error。

## 未做：Tabler 圖示子集化（需建置工具）
- 目標 817KB → ~10KB。但本機 `python` 為 Windows Store 佔位程式、無 `pip`/`fontTools`，無法在此環境子集化 woff2。
- 建議方案（擇一，待你環境就緒再做）：(a) 有 Python 時用 `fonttools` `pyftsubset` 依實際使用的 ~40 個字符子集化；(b) 改為 inline SVG sprite（移除整包字型，但需改寫所有 `<i class="ti">` 用法，工程較大）。

## 風險與回滾
- 圖表/ RWD 為純前端；備忘錄分頁後端僅改查詢與回應形狀（前端同步），不動資料。回滾將 `/memos` 改回回傳陣列、`renderMemos` 改回一次載入即可。
